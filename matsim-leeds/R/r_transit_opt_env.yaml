name: r-transit-opt
channels:
  - conda-forge
dependencies:
  - r-base=4.2
  - r-tidyverse
  - r-sf
  - r-glue
  - r-arrow
  - r-jsonlite     
  - r-curl  # Required for installing packages from R
  - r-remotes  # For installing from GitHub if needed