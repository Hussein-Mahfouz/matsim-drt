name: r-transit-opt
channels:
  - conda-forge
dependencies:
  - r-base=4.2
  - r-tidyverse
  - r-sf
  - r-glue
  - r-arrow
  - r-jsonlite     
  - r-stplanr
  - r-curl  # Required for installing packages from R
  - r-remotes  # For installing from GitHub if needed
  # System libraries needed for spatial packages
  - proj
  - geos
  - gdal
  - udunits2
  - liblwgeom  # Needed for lwgeom R package